<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.taskmanager">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MenuDrawer.SwitchAccountsActivity"></activity>
        <activity android:name=".MenuDrawer.SettingsActivity" />
        <activity android:name=".MenuDrawer.BankAccountActivity" />
        <activity android:name=".MenuDrawer.ProfileActivity" />
        <activity android:name=".CreateHouseActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".ChildUI.HomeChildActivity" />
        <activity android:name=".MainLoginActivity" />
        <activity android:name=".SplashScreen" />
        <activity android:name=".RegisterActivity" />
        <activity
            android:name=".ParentUI.HomeActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>